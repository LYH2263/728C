<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.steam.mapper.GameMapper">

    <!-- 条件查询游戏列表 -->
    <select id="findByCondition" resultType="com.steam.entity.Game">
        SELECT DISTINCT g.*
        FROM games g
        <if test="condition.categoryId != null">
            INNER JOIN game_categories gc ON g.id = gc.game_id
        </if>
        WHERE g.status = 1
        <if test="condition.keyword != null and condition.keyword != ''">
            AND (g.title LIKE CONCAT('%', #{condition.keyword}, '%')
                OR g.description LIKE CONCAT('%', #{condition.keyword}, '%')
                OR g.developer LIKE CONCAT('%', #{condition.keyword}, '%')
                OR g.publisher LIKE CONCAT('%', #{condition.keyword}, '%'))
        </if>
        <if test="condition.categoryId != null">
            AND gc.category_id = #{condition.categoryId}
        </if>
        <if test="condition.minPrice != null">
            AND COALESCE(g.discount_price, g.original_price) >= #{condition.minPrice}
        </if>
        <if test="condition.maxPrice != null">
            AND COALESCE(g.discount_price, g.original_price) &lt;= #{condition.maxPrice}
        </if>
        <if test="condition.onSale != null and condition.onSale == true">
            AND g.discount_percent > 0
        </if>
        <if test="condition.featured != null and condition.featured == true">
            AND g.is_featured = 1
        </if>
        <choose>
            <when test="condition.sortBy == 'price'">
                ORDER BY COALESCE(g.discount_price, g.original_price)
            </when>
            <when test="condition.sortBy == 'rating'">
                ORDER BY g.rating
            </when>
            <when test="condition.sortBy == 'sales'">
                ORDER BY g.sales_count
            </when>
            <when test="condition.sortBy == 'releaseDate'">
                ORDER BY g.release_date
            </when>
            <otherwise>
                ORDER BY g.created_at
            </otherwise>
        </choose>
        <choose>
            <when test="condition.sortOrder == 'asc'">
                ASC
            </when>
            <otherwise>
                DESC
            </otherwise>
        </choose>
        LIMIT #{condition.offset}, #{condition.limit}
    </select>

    <!-- 统计符合条件的游戏总数 -->
    <select id="countByCondition" resultType="java.lang.Long">
        SELECT COUNT(DISTINCT g.id)
        FROM games g
        <if test="condition.categoryId != null">
            INNER JOIN game_categories gc ON g.id = gc.game_id
        </if>
        WHERE g.status = 1
        <if test="condition.keyword != null and condition.keyword != ''">
            AND (g.title LIKE CONCAT('%', #{condition.keyword}, '%')
                OR g.description LIKE CONCAT('%', #{condition.keyword}, '%')
                OR g.developer LIKE CONCAT('%', #{condition.keyword}, '%')
                OR g.publisher LIKE CONCAT('%', #{condition.keyword}, '%'))
        </if>
        <if test="condition.categoryId != null">
            AND gc.category_id = #{condition.categoryId}
        </if>
        <if test="condition.minPrice != null">
            AND COALESCE(g.discount_price, g.original_price) >= #{condition.minPrice}
        </if>
        <if test="condition.maxPrice != null">
            AND COALESCE(g.discount_price, g.original_price) &lt;= #{condition.maxPrice}
        </if>
        <if test="condition.onSale != null and condition.onSale == true">
            AND g.discount_percent > 0
        </if>
        <if test="condition.featured != null and condition.featured == true">
            AND g.is_featured = 1
        </if>
    </select>
</mapper>
